<div id="header">
  <h1 id="header-title">
    <%=link_to "[ B o o k m a k ]", :controller => 'top' %>
  </h1>
  <div id="header-user">
    <% if logged_in? %>
      hello, <%=link_to current_user.name, home_path %>
      (<%=link_to "Sign out", signout_path  %>)
    <% else %>
      <%=link_to "Sign in", signin_path %>
    <% end %>
  </div>
  <br style="clear:both;"/>
</div>
